# 스프라이트시트 제작 인수인계서

이 문서는 이후 다른 에이전트가 캐릭터 스프라이트시트, 공격 애니메이션, VFX 분리 작업을 이어받을 때 참고하기 위한 작업 기준서입니다. 목표는 “예쁜 한 장”이 아니라, Godot에서 실제로 재생했을 때 크기, 피벗, 프레임 일관성, 투명 배경, VFX 분리가 안정적인 런타임 스프라이트시트를 만드는 것입니다.

## 핵심 원칙

1. 캐릭터 본체와 공격 이펙트는 가능하면 분리합니다.
2. 스프라이트시트 셀 크기는 캐릭터의 최대 동작 범위를 기준으로 잡습니다.
3. 프레임별 본체 크기는 기존 캐릭터 시트와 비교해 유지합니다.
4. 공격 궤적 때문에 캐릭터가 작아지거나 잘리면 안 됩니다.
5. 자동 마스크 처리 시 머리카락, 얼굴, 의상 하이라이트를 이펙트로 오인하지 않도록 주의합니다.

## 기존 츠키 기준

츠키 기본 스프라이트시트:

- 런타임 경로: `project-a/assets/characters/spritesheets/tsuki_spritesheet.png`
- 기존 셀 크기: 181x181
- 기존 구조: 4열 기반
- Idle 기준 본체 높이: 약 173px
- 일반 애니메이션 평균 본체 높이: 약 150~170px

공격 모션을 새로 만들 때는 이 본체 크기감을 기준으로 삼아야 합니다. 생성 이미지가 아무리 좋아도 프레임 안에서 츠키가 120px 이하로 줄어들면 게임 안에서는 확실히 작아 보입니다.

## FiveSlash 작업에서 얻은 교훈

FiveSlash는 상단, 중단, 하단, 상단, 중단 순서의 5단 베기 애니메이션입니다.

최종 구성:

- 프레임 수: 24
- 구조: 4열 x 6행
- 최종 셀 크기: 320x320
- 최종 시트 크기: 1280x1920
- 애니메이션 이름: `FiveSlash`
- 재생 속도: 22fps

처음에는 181x181 셀에 캐릭터와 슬래시 이펙트를 함께 넣었지만, 이 방식은 실패에 가까웠습니다. 큰 슬래시 궤적까지 셀 안에 맞추면서 츠키 본체가 작아졌고, 이후 본체 크기를 키우면 머리카락/다리/검이 잘렸습니다.

최종적으로는 320x320 셀을 사용해 충분한 여백을 확보했고, 캐릭터 본체 시트와 슬래시 VFX 시트를 분리했습니다.

## 권장 생성 방식

### 캐릭터 본체 시트

생성 프롬프트에는 다음 내용을 명확히 포함하는 것이 좋습니다.

- 같은 캐릭터, 같은 의상, 같은 무기 유지
- 2D anime game sprite sheet
- transparent or chromakey background
- consistent character scale across frames
- full body visible in every frame
- no cropped hair, feet, weapon, or clothes
- frame grid layout, e.g. 4 columns x 6 rows
- action sequence order 명시

예시 방향:

```text
Create a 2D anime game sprite sheet for Tsuki performing a fast five-hit sword combo.
Use a 4 columns by 6 rows grid, 24 frames total.
Keep Tsuki's full body visible in every frame.
Maintain consistent character scale and outfit details.
Do not crop hair, feet, sword, or clothing.
Use a clean chromakey green background.
The attack order is high slash, mid slash, low slash, high slash, mid slash, with anticipation and recovery frames.
Minimize large slash effects on the character sheet; effects will be separated into VFX.
```

### VFX 시트

VFX는 캐릭터와 별도로 만들거나, 생성 결과에서 슬래시 궤적만 추출합니다.

권장 기준:

- 캐릭터 본체와 다른 파일로 관리
- 같은 프레임 수를 사용하면 타이밍 맞추기 쉬움
- VFX는 `one_shot_vfx.gd`로 한 번 재생 후 자동 삭제 가능
- VFX 위치는 `slash_fx_offset`, 씬 내부 `AnimatedSprite2D.position`, scale로 조정

## 후처리 기준

### 배경 제거

크로마키 배경을 제거할 때는 초록색 배경만 제거해야 합니다.

주의:

- 머리카락 하이라이트
- 흰 의상
- 검날
- 얼굴 주변 밝은 픽셀

이 픽셀들은 슬래시 이펙트와 색이 비슷할 수 있습니다. 색상만으로 파란/흰 픽셀을 모두 제거하면 캐릭터 머리나 몸이 뜯겨 나갑니다.

### 이펙트 제거

캐릭터 본체 시트에서 슬래시를 제거할 때는 “색”보다 “위치와 연결성”을 우선합니다.

권장 방식:

- 본체와 연결된 픽셀은 최대한 보존
- 몸에서 떨어진 긴 파란/흰 궤적만 제거
- 큰 슬래시 면은 제거
- 검 주변에 붙은 얇은 흰 잔선은 일부 남겨도 됨

본체 보존이 VFX 완전 분리보다 우선입니다. 이펙트를 너무 깔끔하게 지우려다 캐릭터 얼굴, 머리카락, 의상이 깨지면 더 큰 문제입니다.

## 크기와 피벗 검수

생성 후 반드시 프레임별 알파 바운딩 박스를 계산합니다.

확인할 값:

- 프레임별 본체 높이
- 평균 높이
- 최소 높이
- 가장자리 여백
- 셀 밖으로 잘리는 픽셀 여부

FiveSlash 최종 기준:

- 본체 높이 범위: 약 154~174px
- 평균 높이: 약 167px
- 가장자리 잘림 위험: 없음

기존 츠키보다 작아 보이면 안 됩니다. 반대로 너무 크게 키워서 셀 가장자리와 닿으면 Godot에서 재생할 때 머리카락, 검, 발끝이 잘립니다.

## Godot 적용 기준

### AnimatedSprite2D / SpriteFrames

Godot에서는 스프라이트시트 셀 크기를 바꾸면 AtlasTexture region도 반드시 같이 바꿔야 합니다.

예:

- 181x181 시트: `Rect2(x, y, 181, 181)`
- 320x320 시트: `Rect2(x, y, 320, 320)`

FiveSlash는 320x320 기준입니다.

첫 프레임:

```gdscript
region = Rect2(0, 0, 320, 320)
```

마지막 프레임:

```gdscript
region = Rect2(960, 1600, 320, 320)
```

### 카드 모션 데이터

카드별 츠키 애니메이션은 데이터 테이블에서 관리합니다.

원본 테이블:

- `project-a/tables/excel/CharacterCards.xlsx`

생성 스크립트:

- `project-a/tools/table_exporter/create_character_cards.py`

런타임 JSON:

- `project-a/data/generated/character_cards.json`

사용 컬럼:

- `motion_animation`

현재 기본값:

- 공격 카드: `Attack`
- 스킬/강화 카드: `Idle`

특정 카드를 FiveSlash로 연결하려면 카드 테이블에서 해당 카드의 `motion_animation`을 `FiveSlash`로 바꿉니다.

## 런타임 코드 흐름

주요 파일:

- `project-a/scripts/data/card_data.gd`
- `project-a/scripts/core/ingame.gd`
- `project-a/scripts/player/heroine_controller.gd`

흐름:

1. `CharacterCards.xlsx`에서 `motion_animation`을 관리
2. exporter가 `character_cards.json` 생성
3. `ingame.gd`가 카드 데이터를 로드해 `CardData.motion_animation`에 저장
4. 카드 사용 시 `motion_animation`을 heroine에게 전달
5. `heroine_controller.gd`의 `play_card_animation()`이 해당 애니메이션 재생

## 새 공격 애니메이션 추가 절차

1. 새 스프라이트시트를 생성합니다.
2. 캐릭터 본체와 VFX를 분리합니다.
3. 본체 시트의 셀 크기와 프레임 수를 확정합니다.
4. Godot `heroine.tscn`에 AtlasTexture 프레임을 추가합니다.
5. SpriteFrames에 새 애니메이션 이름을 추가합니다.
6. 필요하면 VFX 씬을 `project-a/scenes/vfx/` 아래에 추가합니다.
7. `heroine_controller.gd`에서 새 애니메이션명에 맞는 VFX 씬을 연결합니다.
8. `CharacterCards.xlsx`의 `motion_animation`에 새 애니메이션명을 입력합니다.
9. `table_exporter.py`로 generated JSON을 다시 생성합니다.
10. Godot에서 Reimport 또는 Reload Current Project 후 재생 확인합니다.

## 검수 체크리스트

작업 완료 전 아래를 확인합니다.

- 모든 프레임에서 머리카락이 비어 있지 않은가?
- 얼굴/눈/의상 하이라이트가 뜯기지 않았는가?
- 발끝, 검, 머리카락, 치마가 셀 밖으로 잘리지 않는가?
- 기존 츠키보다 작아 보이지 않는가?
- 프레임별 피벗이 심하게 흔들리지 않는가?
- 공격 방향과 발 위치가 자연스러운가?
- VFX가 본체 시트에 과하게 남아 있지 않은가?
- VFX를 지우느라 본체가 손상되지 않았는가?
- Godot AtlasTexture region이 실제 셀 크기와 일치하는가?
- 카드 테이블의 `motion_animation` 값이 실제 SpriteFrames 애니메이션 이름과 일치하는가?

## 자주 생긴 문제와 대응

### 츠키가 작아 보임

원인:

- 큰 슬래시 궤적까지 한 셀에 맞추며 본체가 축소됨

대응:

- 셀 크기를 키움
- 캐릭터 본체와 VFX 분리
- 본체 높이를 기존 시트와 비교

### 머리카락이 비어 있음

원인:

- 파란/흰 계열 픽셀을 슬래시로 오인해 제거함

대응:

- 본체와 연결된 픽셀은 보존
- 색상 기준만으로 제거하지 않음
- 큰 분리된 궤적만 제거

### 몸이나 다리가 잘림

원인:

- 셀 크기가 작거나 프레임 크롭이 타이트함

대응:

- 181x181 대신 320x320처럼 여유 있는 셀 사용
- AtlasTexture region도 셀 크기에 맞게 수정

### VFX 위치가 어긋남

원인:

- VFX 씬 offset 또는 `slash_fx_offset` 불일치

대응:

- `heroine_controller.gd`의 `slash_fx_offset` 조정
- VFX 씬 내부 `AnimatedSprite2D.position` 조정
- 필요하면 애니메이션별 VFX offset 데이터화

## 앞으로 개선하면 좋은 점

- `motion_animation`뿐 아니라 `motion_vfx_scene`, `motion_vfx_offset`, `impact_delay`, `recover_delay`도 데이터 테이블화
- 애니메이션별 VFX 매핑을 `heroine_controller.gd` 하드코딩에서 Resource 또는 테이블 기반으로 이동
- 프레임별 바운딩 박스 검수 스크립트를 정식 도구화
- Godot 에디터에서 SpriteFrames 자동 생성 스크립트 추가

현재 구조는 카드별 애니메이션 선택은 데이터화되어 있지만, 애니메이션별 VFX 선택은 아직 일부 코드에 남아 있습니다. 카드/스킬 수가 늘어나면 이 부분도 다음 리팩터링 대상으로 보는 것이 좋습니다.
