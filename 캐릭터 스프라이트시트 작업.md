# 캐릭터 스프라이트시트 작업

## 개요

이 문서는 카드 마술사 캐릭터와 메인 캐릭터 츠키의 스프라이트시트/애니메이션 작업 기록을 정리한 문서입니다. 작업 중에는 Godot 프로젝트의 런타임 에셋, 원본 보관용 아트 소스, 애니메이션 씬 연결, VFX 분리 방향을 함께 다뤘습니다.

## 카드 마술사 캐릭터

- 참고 이미지의 특징을 바탕으로 카드 마술사 캐릭터 스프라이트시트를 제작했습니다.
- 플레이어블 캐릭터 또는 몬스터 여부가 아직 확정되지 않았기 때문에, 우선 캐릭터 씬과 기본 애니메이션 구성을 중심으로 작업했습니다.
- Idle 프레임에서 하반신이 잘리는 문제를 확인했고, 스프라이트 프레임 크롭 기준을 재검토했습니다.
- Walk/Run 애니메이션에서 한쪽 발만 내딛는 문제가 있어 츠키의 걷기 프레임 구조와 비교하며 보정 방향을 잡았습니다.
- 작업 결과는 성격별로 분류해 커밋/푸시했습니다.

## 츠키 FiveSlash 애니메이션

### 목표

츠키가 상단, 중단, 하단, 상단, 중단 순서로 빠르게 5번 베는 공격 애니메이션을 추가하는 것이 목표였습니다. 자연스러운 5단 베기를 위해 24프레임 구성을 선택했습니다.

### 프레임 구성

- 프레임 수: 24프레임
- 시트 구성: 4열 x 6행
- 최종 셀 크기: 320x320
- 최종 시트 크기: 1280x1920
- 애니메이션 이름: `FiveSlash`
- 재생 속도: 22fps

처음에는 181x181 셀에 캐릭터와 슬래시 이펙트를 함께 넣었지만, 이 방식은 큰 슬래시 궤적까지 셀 안에 맞추느라 츠키 본체가 기존보다 작아 보이는 문제가 있었습니다. 이후 캐릭터 본체와 슬래시 VFX를 분리하는 방향으로 바꿨습니다.

### 본체 시트 보정

최종 본체 시트는 다음 기준으로 보정했습니다.

- 츠키 본체 크기를 기존 시트와 비슷하게 유지했습니다.
- 181x181 셀 대신 320x320 셀을 사용해 머리카락, 다리, 검, 의상이 잘리지 않도록 했습니다.
- 본체가 작아 보이던 프레임은 높이 기준으로 다시 맞췄습니다.
- 마지막 보정에서는 머리/몸/의상 하이라이트를 보존하고, 큰 파란/흰 슬래시 면만 제거했습니다.
- 몸과 검에 붙어 있는 아주 얇은 흰 잔선은 일부 남겼습니다. 이 부분까지 강제로 제거하면 머리카락이나 의상 픽셀이 다시 뜯길 위험이 있어 본체 보존을 우선했습니다.

최종 확인값:

- 프레임 높이 범위: 154~174px
- 평균 프레임 높이: 약 167px
- 가장자리 잘림 위험 프레임: 없음

### 분리된 VFX

슬래시 궤적은 본체 스프라이트시트에서 분리해 별도 VFX 시트와 씬으로 구성했습니다.

- VFX 시트: `project-a/assets/vfx/fx_tsuki_five_slash.png`
- VFX 씬: `project-a/scenes/vfx/fx_tsuki_five_slash.tscn`
- VFX 프레임 수: 24프레임
- VFX 재생 속도: 22fps
- 기존 `one_shot_vfx.gd`를 사용해 한 번 재생 후 자동 삭제되도록 구성했습니다.

## Godot 연결

### Heroine Scene

`project-a/scenes/player/heroine.tscn`에 `FiveSlash` 애니메이션을 추가했습니다.

- `tsuki_five_slash_spritesheet.png`를 외부 텍스처 리소스로 등록했습니다.
- `AtlasTexture_five_slash_00`부터 `AtlasTexture_five_slash_23`까지 24개 프레임을 등록했습니다.
- 각 프레임 영역은 최종 시트 기준인 320x320으로 맞췄습니다.
- `FiveSlash` 애니메이션은 loop false, speed 22.0으로 설정했습니다.

### Heroine Controller

`project-a/scripts/player/heroine_controller.gd`에 FiveSlash 재생 경로를 추가했습니다.

- `play_five_slash_animation(target_position: Variant = null)`을 추가했습니다.
- `_start_attack()`이 공격 애니메이션 이름을 받을 수 있도록 확장했습니다.
- 일반 `Attack`은 기존 `fx_heroine_slash.tscn`을 사용합니다.
- `FiveSlash`는 새 `fx_tsuki_five_slash.tscn`을 사용합니다.
- 공격 애니메이션 종료 처리는 `Attack` 이름만 확인하던 방식에서 `is_attacking` 상태 기반으로 변경했습니다.

## 주요 파일

- `project-a/assets/characters/spritesheets/tsuki_five_slash_spritesheet.png`
- `project-a/assets/characters/spritesheets/tsuki_five_slash_spritesheet.png.import`
- `project-a/assets/vfx/fx_tsuki_five_slash.png`
- `project-a/assets/vfx/fx_tsuki_five_slash.png.import`
- `project-a/scenes/vfx/fx_tsuki_five_slash.tscn`
- `project-a/scenes/player/heroine.tscn`
- `project-a/scripts/player/heroine_controller.gd`
- `Docs/ArtSource/characters/spritesheets/tsuki_five_slash_spritesheet_transparent.png`
- `Docs/ArtSource/characters/spritesheets/tsuki_five_slash_spritesheet_chromakey_source.png`
- `Docs/ArtSource/vfx/fx_tsuki_five_slash.png`

## 남은 주의사항

- Godot에서 에셋이 바로 보이지 않으면 파일시스템 독에서 Reimport하거나 `Project > Reload Current Project`를 실행하면 됩니다.
- 현재 본체 보존을 우선했기 때문에 일부 흰 잔선은 본체 시트에 남아 있습니다.
- VFX 위치가 실제 전투 화면에서 어긋나면 `slash_fx_offset`, `attack_impact_delay`, 또는 `fx_tsuki_five_slash.tscn`의 `AnimatedSprite2D` 위치를 조정하면 됩니다.
